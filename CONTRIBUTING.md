# 参与贡献

感谢参与维护。本仓库默认使用 Issue 记录缺陷和需求。

## 提交前
1. 先搜索现有 Issue，避免重复。
2. 缺陷请提供版本、Windows 版本、复现步骤、样本格式及截图。
3. 样本应移除密码、Token、设备序列号和隐私数据。

## 开发流程
1. 从 `main` 创建分支。
2. 仅修改目标项目目录，避免混入无关文件。
3. 运行项目测试和语法检查。
4. 提交清晰的变更说明，再发起 Pull Request。

提交信息建议使用 `feat:`、`fix:`、`docs:`、`test:`、`style:`、`refactor:` 前缀。
